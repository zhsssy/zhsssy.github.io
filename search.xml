<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>go-micro(1)</title>
    <url>/2020/06/06/go-micro(1)/</url>
    <content><![CDATA[<h1 id="Go-Micro"><a href="#Go-Micro" class="headerlink" title="Go-Micro"></a>Go-Micro</h1><h2 id="特点"><a href="#特点" class="headerlink" title="特点"></a>特点</h2><ul>
<li>可插拔<ul>
<li>可以直接go-micro的默认实现或者在<a href="https://github.com/micro/go-plugins" target="_blank" rel="noopener">go-plugins</a>中选择基于不同组件实现的插件库,甚至可以基于go-micro框架抽象的接口来实现自己的插件库</li>
</ul>
</li>
<li>更容易扩展</li>
<li>专注一个业务功能</li>
<li>耦合松散，维护简单</li>
</ul>
<a id="more"></a>

<h2 id="Go-Micro系统架构"><a href="#Go-Micro系统架构" class="headerlink" title="Go-Micro系统架构"></a>Go-Micro系统架构</h2><p><img src="./pic1.png" alt=""></p>
<p><img src="./pic2.jpeg" alt=""></p>
<h3 id="Registry"><a href="#Registry" class="headerlink" title="Registry"></a>Registry</h3><p>注册表提供可插入的服务发现库，来查找正在运行的服务。当前的实现是consul，etcd，内存和kubernetes。如果您的喜欢不一样，该界面很容易实现。</p>
<h3 id="Selector"><a href="#Selector" class="headerlink" title="Selector"></a>Selector</h3><p>选择器通过选择提供负载均衡机制。当客户端向服务器发出请求时，它将首先查询服务的注册表。这通常会返回一个表示服务的正在运行的节点列表。选择器将选择这些节点中的一个用于查询。多次调用选择器将允许使用平衡算法。目前的方法是循环法，随机哈希和黑名单。</p>
<h3 id="Broker"><a href="#Broker" class="headerlink" title="Broker"></a>Broker</h3><p>Broker是发布和订阅的可插入接口。微服务是一个事件驱动的架构，发布和订阅事件应该是一流的公民。目前的实现包括nats，rabbitmq和http（用于开发）。</p>
<h3 id="Transport"><a href="#Transport" class="headerlink" title="Transport"></a>Transport</h3><p>传输是通过点对点传输消息的可插拔接口。目前的实现是http，rabbitmq和nats。通过提供这种抽象，运输可以无缝地换出。</p>
<h3 id="Codec"><a href="#Codec" class="headerlink" title="Codec"></a>Codec</h3><p>编码</p>
<h3 id="Client"><a href="#Client" class="headerlink" title="Client"></a>Client</h3><p>客户端提供了一种制作RPC查询的方法。它结合了注册表，选择器，代理和传输。它还提供重试，超时，使用上下文等。</p>
<h3 id="Server"><a href="#Server" class="headerlink" title="Server"></a>Server</h3><p>服务器是构建正在运行的微服务的接口。它提供了一种提供RPC请求的方法。</p>
<h3 id="Plugins"><a href="#Plugins" class="headerlink" title="Plugins"></a>Plugins</h3><p>提供go-micro的<a href="https://github.com/micro/go-plugins" target="_blank" rel="noopener">go-plugins</a>插件。</p>
<p><a href="https://github.com/micro/go-plugins" target="_blank" rel="noopener">https://github.com/micro/go-plugins</a></p>
<h4 id="api"><a href="#api" class="headerlink" title="api"></a>api</h4><p>micro api 构建在 go-micro 上，利用它进行服务发现，负载平衡，编码和基于 RPC 的通信。对 API 的请求通过 HTTP 提供，并通过 RPC 进行内部路由.</p>
<p>由于 micro api 在内部使用 go-micro, 因此它也支持插件，因此可以随时切换为 kubernetes api 的 consul 服务发现或 gRPC.</p>
<p><img src="./pic3.png" alt=""></p>
<h4 id="web"><a href="#web" class="headerlink" title="web"></a>web</h4><p>Micro Web提供了一个用于查看和查询服务的仪表板.</p>
<p><img src="./pic4.png" alt=""></p>
<h1 id="grpc-NewService和micro-NewService"><a href="#grpc-NewService和micro-NewService" class="headerlink" title="grpc.NewService和micro.NewService()"></a>grpc.NewService和micro.NewService()</h1><p>在<code>go-micro</code>中主要提供了两种创建<code>micro.Service</code>的方式：</p>
<ol>
<li><h2 id="gprc-NewService"><a href="#gprc-NewService" class="headerlink" title="gprc.NewService()"></a><code>gprc.NewService()</code></h2></li>
<li><h2 id="micro-NewService"><a href="#micro-NewService" class="headerlink" title="micro.NewService()"></a><code>micro.NewService()</code></h2></li>
</ol>
<p>第一种方式下底层传输协议使用的是<code>grpc</code>；第二种方式下底层传输协议使用的是<code>http + protobuf</code>。从实际比较来看，<code>gprc</code>的性能明显好于<code>micro</code>。</p>
]]></content>
      <tags>
        <tag>golang</tag>
        <tag>go-micro</tag>
      </tags>
  </entry>
  <entry>
    <title>tar安装软件</title>
    <url>/2020/05/31/tar%E5%AE%89%E8%A3%85%E8%BD%AF%E4%BB%B6/</url>
    <content><![CDATA[<ul>
<li>打开一个SHELL，即终端<br>用cd 命令进入源代码压缩包所在的目录</li>
<li>根据压缩包类型解压缩文件(*代表压缩包名称)<br>tar -xvzf .tar.gz || tar -jxvf .tar.bz(或bz2)</li>
<li>用CD命令进入解压缩后的目录</li>
</ul>
<a id="more"></a>

<ul>
<li>输入编译文件命令：./configure（有的压缩包已经编译过，这一步可以省去）<br>./configure –prefix=/opt 表示安装到/opt目录 。</li>
<li>命令: make</li>
<li>再是安装文件命令：make install</li>
<li>执行“make clean”删除安装时产生的临时文件</li>
<li>安装完毕</li>
</ul>
]]></content>
      <tags>
        <tag>linux</tag>
      </tags>
  </entry>
  <entry>
    <title>pacman常用命令</title>
    <url>/2020/05/31/pacman%E5%B8%B8%E7%94%A8%E5%91%BD%E4%BB%A4/</url>
    <content><![CDATA[<h2 id="更新系统"><a href="#更新系统" class="headerlink" title="更新系统"></a>更新系统</h2><p>在 Archlinux 中，使用一条命令即可对整个系统进行更新：<br>pacman -Syu<br>如果你已经使用pacman -Sy将本地的包数据库与远程的仓库进行了同步，也可以只执行：pacman -Su</p>
<h2 id="安装包"><a href="#安装包" class="headerlink" title="安装包"></a>安装包</h2><p>➔ pacman -S 包名：例如，执行 pacman -S firefox 将安装 Firefox。你也可以同时安装多个包，<br>只需以空格分隔包名即可。<br>➔ pacman -Sy 包名：与上面命令不同的是，该命令将在同步包数据库后再执行安装。<br>➔ pacman -Sv 包名：在显示一些操作信息后执行安装。<br>➔ pacman -U：安装本地包，其扩展名为 pkg.tar.gz。<br>➔ pacman -U <a href="http://www.example.com/repo/example.pkg.tar.xz" target="_blank" rel="noopener">http://www.example.com/repo/example.pkg.tar.xz</a> 安装一个远程包（不在 pacman 配置的源里面）</p>
<a id="more"></a>显示在这里<!--more-->
<h2 id="删除包"><a href="#删除包" class="headerlink" title="删除包"></a>删除包</h2><p>➔ pacman -R 包名：该命令将只删除包，保留其全部已经安装的依赖关系<br>➔ pacman -Rs 包名：在删除包的同时，删除其所有没有被其他已安装软件包使用的依赖关系<br>➔ pacman -Rsc 包名：在删除包的同时，删除所有依赖这个软件包的程序<br>➔ pacman -Rd 包名：在删除包时不检查依赖。</p>
<h2 id="搜索包"><a href="#搜索包" class="headerlink" title="搜索包"></a>搜索包</h2><p>➔ pacman -Ss 关键字：在仓库中搜索含关键字的包。<br>➔ pacman -Qs 关键字： 搜索已安装的包。<br>➔ pacman -Qi 包名：查看有关包的详尽信息。<br>➔ pacman -Ql 包名：列出该包的文件。</p>
<h2 id="其他用法"><a href="#其他用法" class="headerlink" title="其他用法"></a>其他用法</h2><p>➔ pacman -Sw 包名：只下载包，不安装。<br>➔ pacman -Sc：清理未安装的包文件，包文件位于 /var/cache/pacman/pkg/ 目录。<br>➔ pacman -Scc：清理所有的缓存文件。</p>
]]></content>
      <tags>
        <tag>archlinux</tag>
      </tags>
  </entry>
  <entry>
    <title>go语言中的interface</title>
    <url>/2020/03/19/interface/</url>
    <content><![CDATA[<h1 id="interface-是一种类型"><a href="#interface-是一种类型" class="headerlink" title="interface 是一种类型"></a>interface 是一种<strong>类型</strong></h1><p><strong>interface 是一种类型</strong>，从它的定义可以看出来用了 type 关键字，更准确的说 interface 是一种<strong>具有一组方法的类型</strong>。</p>
<p><strong>如果一个类型实现了一个 interface 中所有方法，我们说类型实现了该 interface</strong>，所以所有类型都实现了 empty interface，因为任何一种类型至少实现了 0 个方法。go 没有显式的关键字用来实现 interface，只需要实现 interface 包含的方法即可。</p>
<h2 id="go允许空接口"><a href="#go允许空接口" class="headerlink" title="go允许空接口"></a>go允许空接口</h2><p>go 允许不带任何方法的 interface ，这种类型的 interface 叫 <strong>empty interface</strong>。</p>
<p><code>interface{}</code> 是一个空的 interface 类型，根据前文的定义：一个类型如果实现了一个 interface 的所有方法就说该类型实现了这个 interface，空的 interface 没有方法，所以可以认为所有的类型都实现了 <code>interface{}</code>。如果定义一个函数参数是 <code>interface{}</code> 类型，这个函数应该可以接受任何类型作为它的参数。</p>
<a id="more"></a>

<h2 id="duck-typing"><a href="#duck-typing" class="headerlink" title="duck typing "></a><a href="https://en.wikipedia.org/wiki/Duck_typing" target="_blank" rel="noopener">duck typing </a></h2><p>一个对象有效的语义，不是由继承自特定的类或实现特定的接口，而是由”当前方法和属性的集合”决定 。</p>
<h2 id="类型转换"><a href="#类型转换" class="headerlink" title="类型转换"></a>类型转换</h2><p>在类型转换过程中，由于类型信息不会改变，直接通过拷贝指针赋值，而具体的值则根据原始类型的大小，复制一份新的内容。 </p>
<h3 id="源码-eface"><a href="#源码-eface" class="headerlink" title="源码 eface"></a>源码 eface</h3><figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="keyword">type</span> eface <span class="keyword">struct</span> &#123;</span><br><span class="line">	_type *_type   <span class="comment">//_type是对一种具体类型的描述,例如int,string</span></span><br><span class="line">	data  unsafe.Pointer   <span class="comment">//指向真实数据的指针</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// Needs to be in sync with ../cmd/link/internal/ld/decodesym.go:/^func.commonsize,</span></span><br><span class="line"><span class="comment">// ../cmd/compile/internal/gc/reflect.go:/^func.dcommontype and</span></span><br><span class="line"><span class="comment">// ../reflect/type.go:/^type.rtype.</span></span><br><span class="line"><span class="keyword">type</span> _type <span class="keyword">struct</span> &#123;</span><br><span class="line">	size       <span class="keyword">uintptr</span> <span class="comment">//type size</span></span><br><span class="line">	ptrdata    <span class="keyword">uintptr</span> <span class="comment">// size of memory prefix holding all pointers</span></span><br><span class="line">	hash       <span class="keyword">uint32</span>  <span class="comment">//hash of type;avoids computation in hash table</span></span><br><span class="line">	tflag      tflag</span><br><span class="line">	align      <span class="keyword">uint8</span></span><br><span class="line">	fieldalign <span class="keyword">uint8</span></span><br><span class="line">	kind       <span class="keyword">uint8</span> <span class="comment">// type mask</span></span><br><span class="line">	alg        *typeAlg</span><br><span class="line">	<span class="comment">// gcdata stores the GC type data for the garbage collector.</span></span><br><span class="line">	<span class="comment">// If the KindGCProg bit is set in kind, gcdata is a GC program.</span></span><br><span class="line">	<span class="comment">// Otherwise it is a ptrmask bitmap. See mbitmap.go for details.</span></span><br><span class="line">	gcdata    *<span class="keyword">byte</span></span><br><span class="line">	str       nameOff <span class="comment">//string form</span></span><br><span class="line">	ptrToThis typeOff <span class="comment">// type for pointer to this type, may be zero</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="源码iface-有方法"><a href="#源码iface-有方法" class="headerlink" title="源码iface      有方法"></a>源码iface      有方法</h3><figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="keyword">type</span> iface <span class="keyword">struct</span> &#123;</span><br><span class="line">	tab  *itab   <span class="comment">//复杂，既有数据又有方法</span></span><br><span class="line">	data unsafe.Pointer</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// layout of Itab known to compilers</span></span><br><span class="line"><span class="comment">// allocated in non-garbage-collected memory</span></span><br><span class="line"><span class="comment">// Needs to be in sync with</span></span><br><span class="line"><span class="comment">// ../cmd/compile/internal/gc/reflect.go:/^func.dumptypestructs.</span></span><br><span class="line"><span class="keyword">type</span> itab <span class="keyword">struct</span> &#123;</span><br><span class="line">	inter  *interfacetype <span class="comment">//interface type description     package path method</span></span><br><span class="line">	_type  *_type         <span class="comment">//origin type</span></span><br><span class="line">	link   *itab  <span class="comment">//hash表头指针</span></span><br><span class="line">	hash   <span class="keyword">uint32</span> <span class="comment">// copy of _type.hash. Used for type switches.</span></span><br><span class="line">	bad    <span class="keyword">bool</span>   <span class="comment">// type does not implement interface</span></span><br><span class="line">	inhash <span class="keyword">bool</span>   <span class="comment">// has this itab been added to hash?</span></span><br><span class="line">	unused [<span class="number">2</span>]<span class="keyword">byte</span></span><br><span class="line">	fun    [<span class="number">1</span>]<span class="keyword">uintptr</span> <span class="comment">//method具体实现</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">type</span> interfacetype <span class="keyword">struct</span> &#123;</span><br><span class="line">	typ     _type</span><br><span class="line">	pkgpath name</span><br><span class="line">	mhdr    []imethod</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">type</span> imethod <span class="keyword">struct</span> &#123;</span><br><span class="line">	name nameOff</span><br><span class="line">	ityp typeOff</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h1 id="interface和nil"><a href="#interface和nil" class="headerlink" title="interface和nil"></a>interface和nil</h1><p> 一个interface{}类型的变量包含了2个指针，一个指针指向值的类型，另外一个指针指向实际的值</p>
]]></content>
      <tags>
        <tag>golang</tag>
      </tags>
  </entry>
</search>
